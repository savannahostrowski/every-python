repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      # Run the linter
      - id: ruff
        args: [--fix]
        files: '^every_python/.*\.py$'
      # Run the formatter
      - id: ruff-format
        files: '^every_python/.*\.py$'

  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: uv run --frozen --group dev pytest
        language: system
        pass_filenames: false
        always_run: true
        stages: [commit]
      - id: ty
        name: ty
        entry: uv run ty check every_python/
        language: system
        pass_filenames: false
        types: [python]
        stages: [commit]